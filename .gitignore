.DS_Store

# dev files
build
target/
platform/*.a
platform/*.lib
platform/*.tar.br
platform/*.tar.gz
platform/*.lib
# Built per-target host libs (produced by build.sh, bundled for release).
# The musl toolchain inputs (crt1.o/libc.a/libunwind.a) ARE committed.
platform/targets/*/libhost.a
platform/targets/*/host.lib

# examples
example/main
example/output/
example/**/*.html

# generated files
crates/roc_std/
generated-docs/
